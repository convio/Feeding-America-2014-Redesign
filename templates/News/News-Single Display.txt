<div class="two-column">
  <div class="container">
    <div class="column-1">
      <div id="page-title">
        <h1 class="page-title">${title}</h1>
      </div><!--/#page-title-->
      <t:if test="!isNull(description)"><div id="page-description">
        <p>${description}</p>
      </div></t:if><!--/#page-description-->
      <div id="body-content">
        <t:if test="thumb.length > 0"><t:list id="thumb"><img src="${url}" alt="${alt_text}" class="image-right" /></t:list></t:if>
        ${body}

        <t:if test="secondary_tags.length > 0">
          <t:if test="state.length > 0"><t:list id="state" maxlength="1"><t:value id="relatedItems" value="concat('&amp;state=', name)" /></t:list></t:if>
          <t:if test="food_bank.length > 0"><t:list id="food_bank" maxlength="1"><t:value id="relatedItems" value="concat(relatedItems, '&amp;food_bank=')" /><t:value id="relatedItems" value="concat(relatedItems, itemid)" />
          </t:list></t:if>
          <div class="list-item-tags">Tags: <t:list id="secondary_tags"><a href="http://fa.pub30.convio.net/hunger-in-america/news-and-updates/our-perspective/?secondary_tags=${name}${relatedItems}">${label}</a><t:if test="index != length">, </t:if></t:list></div>
        </t:if>

        <div class="templateComponent" id="commentlist-540352587"></div>
        <h2>Leave a Comment</h2>
        <t:if test="user.isanonymous">
          <p><a href="http://help.feedingamerica.org/site/Login?NEXTURL=${url}">Please login to comment</a></p>
        </t:if>
        <t:else>
          <div class="templateComponent" id="commentform-540352869"></div>
        </t:else>
      </div><!--/#body-content -->
      <t:if test="inline_promo.length > 0"><t:include id="item-540519669" /></t:if>
      <t:else>
        <div class="promos">
          <!-- global promos-->
          <div class="primary-promos">
            <t:include id="templatelist-541303840" />
          </div><!--/.primary-promos -->
          <!-- dynamic section based -->
          <div class="secondary-promos">
            <t:include id="templatelist-541304220" />
          </div><!-- /.secondary-promos -->
        </div><!-- /.promos-->
      </t:else>

    </div><!--/.column-1-->
    <div class="column-2">
      <!-- social share utilities -->
      <t:include id="item-541309505" />

      <!-- Search News -->
      <t:include id="item-536428902"/>

      <!-- Related content -->
      <t:if test="related_content.length > 0">
        <t:include id="templatelist-541315156" />
      </t:if>
      <t:else>
        <!-- section based related content -->
        <div class="promos">
          <!-- news related to the page by news type & tags-->
          <div class="primary-promos">
            <t:include id="templatelist-541315370" />
          </div><!-- /.primary-promos -->
          <!-- general content -->
          <div class="secondary-promos">
            <t:include id="templatelist-541315508" />
          </div><!-- /.secondary-promos -->
        </div><!-- .promos -->
      </t:else>

      <!-- Contextual Sidebar Promo -->
      <t:if test="sidebar_promos.length > 0">
      </t:if>
      <t:else>
        <!-- dynamic contextual sidebar promos & second sidebar promos -->
        <div class="promos">
          <!-- contextual sidebar promos -->
          <div class="primary-promos">
            <!-- global promos -->
            <t:include id="templatelist-541539906" />
          </div>

          <div class="secondary-promos">
            <!-- advanced filtered by section -->
            <t:include id="templatelist-541538772" />
          </div>

        </div><!-- /.promos -->

        <div class="promos">
          <!-- second sidebar promos (put into reusable) -->
          <t:set id="emailConditionalCategory" value="'default-anonymous'" />
          <t:if test="user.isanonymous == false && !isNull(email.primary_address)">
            <t:set id="emailConditionalCategory" value="'email-on-file'" />
          </t:if>

          <div class="primary-promos">
            <!-- global promos -->
            <t:if test="emailConditionalCategory == 'default-anonymous'">
              <!-- global list of default/anon secondary promos -->
              <t:include id="templatelist-541720519" />
            </t:if>
            <t:else>
            <!-- global randomized list of secondary promos tagged with logged in & email on file -->
              <t:include id="templatelist-542289635" />
            </t:else>
          </div>

          <div class="secondary-promos">
            <t:if test="emailConditionalCategory == 'default-anonymous'">
              <!-- generic list of default secondary promos -->
            <t:include id="templatelist-541720172" />
            </t:if>
            <t:else>
              <!-- generic randomized list of secondary promos tagged with logged in & email on file -->
              <t:include id="templatelist-542291343" />
            </t:else>
          </div>
        </div><!-- /.promos -->
      </t:else>

    </div><!--/.column-2-->
    <div class="clearfix"></div>
  </div><!--/.container-->
</div><!--/.two-column-->

<div id="footer_promos">
  <div class="container">
    <div class="headline">How You Can Help Feeding America Solve Hunger</div>
      <div id="footer_promo_boxes">

    <t:if test="footer_promos.length > 0">
      <t:list id="footer_promos">
        <t:set id="buttonClasses" value="'button'" />
        <t:if test="action_button_color.length > 0 || action_button_style.length > 0">
          <t:if test="action_button_style.length > 0">
            <t:list id="action_button_style">
              <t:set id="buttonClasses" value="name" />
            </t:list>
          </t:if>
          <t:if test="action_button_color.length > 0">
            <t:list id="action_button_color">
              <t:set id="buttonClasses" value="concat(concat(buttonClasses, ' '), name)" />
            </t:list>
          </t:if>
        </t:if>
        <div class="footer-promo">
          <t:if test="thumb.length > 0"><t:list id="thumb"><img src="${url}" alt="${alt_text}" /></t:list></t:if>
          <div class="promo-title">${title}</div>
          <t:if test="!isNull(description)"><div class="promo-text">${description}</div></t:if>

          <t:if test="!isNull(action_button_label) && !isNull(action_button_url)"><div class="promo-action"><a class="${buttonClasses}" href="${action_button_url}">${action_button_label}</a></div></t:if>
        </div>
      </t:list>
    </t:if>
    <t:else>
      <!-- dynamic footer promos -->
      <!-- Learn -->
      <t:include id="templatelist-541527765" />
      <!-- Act -->
      <t:include id="templatelist-541527691" />
      <!-- Give -->
      <t:include id="templatelist-541528010" />
    </t:else>

      <div class="clearfix"></div>
    </div><!--/#footer_promo_boxes-->
  </div><!--/.container-->
</div><!--/#footer_promos-->